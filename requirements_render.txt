# Requirements for lightweight Render deployment
# Only fingerprint computation — no ONNX models, no SQLite
fastapi>=0.100
uvicorn[standard]>=0.22
pydantic>=2.0
numpy>=1.24,<2.0
rdkit>=2023.9.1
requests>=2.20
