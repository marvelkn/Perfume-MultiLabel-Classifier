# Local install path (this machine has no conda): python -m venv .venv && pip install -r requirements.txt
# --- Data layer (needed NOW for the build + tests) ---
rdkit>=2023.9.1
pandas>=2.0
numpy>=1.24,<2.0
scipy>=1.10
pyrfume>=0.18
iterative-stratification>=0.1.7
pyyaml>=6.0
# --- EDA ---
matplotlib>=3.7
seaborn>=0.12
jupyterlab>=4.0
# --- Modeling (YOUR phase — listed for convenience) ---
scikit-learn>=1.3
imbalanced-learn>=0.11
xgboost>=2.0
lightgbm>=4.0
# --- ONNX export + desktop runtime parity check (YOUR phase) ---
skl2onnx>=1.16
onnxmltools>=1.12
onnxruntime>=1.17
# --- Testing ---
pytest>=7.4
